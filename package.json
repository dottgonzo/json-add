{
  "name": "json-add",
  "version": "0.0.2",
  "description": "Union of two json",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "hw": "git add -A && git commit -m 'oki'&&npm version patch -m patch&&git add -A && git commit --amend -C HEAD&& git push&&npm publish",
    "patch": "npm version patch -m patch&&git add -A && git commit --amend -C HEAD&& git push&&npm publish",
    "prepublish": "npm test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dottgonzo/json-add.git"
  },
  "keywords": [
    "json",
    "unions"
  ],
  "author": "Dario Caruso",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dottgonzo/json-add/issues"
  },
  "dependencies": {
    "deep-diff": "^0.3.2"
  },
  "devDependencies": {
    "chai": "^3.4.1"
  }
}
